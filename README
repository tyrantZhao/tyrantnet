tyrantnet
=======
A cross platform network library(support Windows and Linux), using c++11(use some C++17 feature), no other library depends.

## Features
* support linux and Windows
* multi-thread safety and high performance
* none extention depend
* ipv6 support
* OpenSSL support(use macro USE-OPENSSL when compiling)
* support http, websocket, https

## Build
* mkdir build
* cmake ../
* make -j4

## Example
* in example/, examples will be added continuely

